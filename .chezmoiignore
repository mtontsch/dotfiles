# Repo-only files, never deployed into $HOME
README.md
install.sh
docs/

# In devcontainers ~/.ssh is a read-only bind mount from the host: don't manage it
{{ if stat "/.dockerenv" -}}
.ssh
.ssh/**
{{- end }}

# Devcontainer baseline: consumed via per-project copies, never deployed by
# chezmoi. (Container shells get the same chezmoi-managed config as hosts —
# the baseline postCreateCommand.sh bootstraps this repo and applies it.)
devcontainer-baseline/
