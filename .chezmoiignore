# Repo-only files, never deployed into $HOME
README.md
install.sh
docs/

# In devcontainers ~/.ssh is a read-only bind mount from the host: don't manage it
{{ if stat "/.dockerenv" -}}
.ssh
.ssh/**
{{- end }}

# Devcontainer baseline + container-shared shell/bin: consumed via the
# devcontainer same-path mount and per-project copies, never deployed by chezmoi
devcontainer-baseline/
shell/
bin/
.gitignore
.zsh_history.local
