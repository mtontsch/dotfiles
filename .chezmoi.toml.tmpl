{{/* Asked once per machine on `chezmoi init`; answer stored in ~/.config/chezmoi/chezmoi.toml */}}
{{- $role := promptStringOnce . "role" "role" -}}

umask = 0o077

[data]
    role = {{ $role | quote }}
